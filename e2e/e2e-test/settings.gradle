pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        google()
        gradlePluginPortal()
    }
    //Eval.xy(this, it, file('../../gradle/repositories.settings.gradle').text)
}
