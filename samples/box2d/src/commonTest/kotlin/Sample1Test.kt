import com.soywiz.korge.tests.*
import com.soywiz.korge.view.*
import com.soywiz.korim.color.*
import kotlin.test.*

class Sample1Test : KorgeTest() {
	@Test
	fun test() {
		/*
		views.stage.apply {
			val rect1 = solidRect(100, 100, Colors.AQUAMARINE)
			val rect2 = solidRect(100, 100, Colors.AQUAMARINE).position(-200, -200)

			assertEquals(true, rect1.isVisibleToUser())
			assertEquals(false, rect2.isVisibleToUser())
		}
		*/
		assertEquals(true, true)
	}
}
